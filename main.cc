// made by Tylerr, simple c++ console program
// Edited on 07/22/2020
#include <iostream>
#include <cmath>

using namespace std;

int main()
{
  int slices; //dec'd but not init'd
  cout << "Yo dumbass how much pieces of pizza u got?: ";
  cin >> slices;
  cout << "You have " << slices << " Slices of pizza" << endl;//returns the number of slices
  cout << pow(10, 2);
  //<< = operator
  //cout = obj / console input out | cin = console input, ex: how many slices: {{inputhere}}
}

